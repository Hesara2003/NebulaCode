# Example environment variables for the NebulaCode backend.
# Copy this file to `.env` (local-only) and update values for your environment.
# DO NOT commit real secrets to the repository.

# Application
# NODE_ENV can be 'development' or 'production'
NODE_ENV=development
# The port the NestJS app listens on
PORT=4000

# Postgres database (used when TypeORM is enabled)
# When using docker-compose the service name is `postgres`
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=nebula
DATABASE_PASSWORD=nebula_password
DATABASE_NAME=nebulacode

# Alternatively, you can provide a single DATABASE_URL:
# DATABASE_URL=postgresql://nebula:nebula_password@postgres:5432/nebulacode

# Redis (used for presence / caching)
REDIS_HOST=redis
REDIS_PORT=6379

# Optional secrets (keep these out of source control). Example only.
# JWT_SECRET=replace_with_a_strong_secret
# SESSION_SECRET=replace_with_a_strong_secret

